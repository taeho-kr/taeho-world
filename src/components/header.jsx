import styled from "styled-components"

const Header = () => {
    return (
        <ComponentWrapper>
            <a href="/">header</a>
        </ComponentWrapper>
    )
}

const ComponentWrapper = styled.div`
    
`

export default Header